(() => {
  const request = async (url, options) => { const r = await fetch(url, options); const d = await r.json(); if (!r.ok || !d.success) throw new Error(d.message || 'Permintaan gagal'); return d.data; };
  const toast = (theme, title, desc) => window.showToast ? showToast({ theme, title, desc }) : alert(`${title}: ${desc}`);
  const date = value => value ? new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value)) : '-';
  const pinList = document.querySelector('#pin-list');
  const loadPins = async () => { if (!pinList) return; try { const pins = await request(pinList.dataset.listEndpoint); pinList.innerHTML = pins.length ? pins.map(p => `<div class="credential-row pin-grid" data-id="${p.id}"><span>${p.roomName}</span><span class="status-pill ${p.isActive ? 'active' : 'inactive'}">${p.isActive ? 'Aktif' : 'Nonaktif'}</span><span>${date(p.createdAt)}</span><span><button class="btn btn-sm btn-outline-primary pin-edit">Ubah</button> <button class="btn btn-sm btn-outline-danger pin-delete">Hapus</button></span></div>`).join('') : '<p class="text-center text-muted py-4 mb-0">Belum ada PIN terdaftar.</p>'; } catch(e) { pinList.innerHTML=`<p class="text-center text-danger py-4 mb-0">${e.message}</p>`; } };
  document.querySelector('#pin-form')?.addEventListener('submit', async e => { e.preventDefault(); try { await request(pinList.dataset.createEndpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}); e.currentTarget.reset(); toast('success','PIN tersimpan','PIN mandiri berhasil dibuat.'); loadPins(); } catch(err) { toast('danger','Gagal menyimpan PIN',err.message); } });
  pinList?.addEventListener('click', async e => { const row=e.target.closest('[data-id]'); if (!row) return; const id=row.dataset.id; try { if(e.target.closest('.pin-delete') && confirm('Hapus PIN ini?')) { await request(`${pinList.dataset.deleteEndpoint}/${id}`,{method:'DELETE'}); loadPins(); } if(e.target.closest('.pin-edit')) { const newPin=prompt('Masukkan PIN baru (6 digit):'); if(/^\d{6}$/.test(newPin||'')) { await request(`${pinList.dataset.updateEndpoint}/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({newPin})}); toast('success','PIN diperbarui','PIN berhasil diperbarui.'); } } } catch(err) { toast('danger','Permintaan gagal',err.message); } });
  const fpList=document.querySelector('#fingerprint-list');
  const loadFingerprint=async()=>{ if(!fpList)return; try { const rows=await request(fpList.dataset.listEndpoint); fpList.innerHTML=rows.length?rows.map(x=>`<div class="credential-row fingerprint-grid" data-id="${x.id}"><span>${x.username||'-'}</span><span>${x.roomName}</span><span>${x.deviceId}</span><span>${x.fingerId}</span><span class="status-pill ${x.isActive?'active':'inactive'}">${x.isActive?'Aktif':'Nonaktif'}</span><span><button class="btn btn-sm btn-outline-primary fp-toggle">${x.isActive?'Nonaktifkan':'Aktifkan'}</button> <button class="btn btn-sm btn-outline-danger fp-delete">Hapus</button></span></div>`).join(''):'<p class="text-center text-muted py-4 mb-0">Belum ada fingerprint terdaftar.</p>'; } catch(e){fpList.innerHTML=`<p class="text-center text-danger py-4 mb-0">${e.message}</p>`;} };
  fpList?.addEventListener('click',async e=>{const row=e.target.closest('[data-id]');if(!row)return;try{if(e.target.closest('.fp-delete')&&confirm('Hapus mapping fingerprint ini?')){await request(`${fpList.dataset.deleteEndpoint}/${row.dataset.id}`,{method:'DELETE'});loadFingerprint();}if(e.target.closest('.fp-toggle')){const active=e.target.textContent.trim()!=='Aktifkan';await request(`${fpList.dataset.updateEndpoint}/${row.dataset.id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({isActive:!active})});loadFingerprint();}}catch(err){toast('danger','Permintaan gagal',err.message);}});
  document.querySelector('#fingerprint-enroll-start')?.addEventListener('click',()=>toast('warning','Enroll belum tersedia','Endpoint registrasi fingerprint masih belum diaktifkan di backend.'));
  document.querySelector('#face-registration-form')?.addEventListener('submit',e=>{e.preventDefault();toast('warning','Bridge MQTT diperlukan','Service MobileFaceNet menerima registrasi melalui MQTT. Tambahkan endpoint Node.js untuk menerbitkan command dari halaman ini.');});
  loadPins();loadFingerprint();
})();
